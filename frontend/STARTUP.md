# 前端项目启动指南

## 📦 安装依赖

首先进入前端目录：

```bash
cd frontend
```

安装依赖（推荐使用 pnpm，速度更快）：

```bash
# 使用 npm
npm install

# 或使用 pnpm (推荐)
pnpm install

# 或使用 yarn
yarn install
```

## 🚀 启动开发服务器

```bash
npm run dev
```

访问 http://localhost:5173

默认会代理后端API到 http://localhost:8080

## 🏗️ 构建生产版本

```bash
npm run build
```

构建产物在 `dist/` 目录。

## 👀 预览生产构建

```bash
npm run preview
```

## 📝 默认账号

在后端服务运行后，可以使用以下账号登录：

- **用户名**: admin
- **密码**: admin123
- **认证方式**: 本地认证

## 🔧 配置说明

### 环境变量

项目根目录下有两个环境变量文件：

- `.env.development` - 开发环境配置
- `.env.production` - 生产环境配置

主要配置项：

```bash
# 后端 API 地址
VITE_API_BASE_URL=http://localhost:8080
```

### 代理配置

开发环境下，Vite 会自动将 `/api` 请求代理到后端服务。

配置在 `vite.config.ts` 中：

```typescript
server: {
  proxy: {
    '/api': {
      target: 'http://localhost:8080',
      changeOrigin: true,
    },
  },
}
```

## 🎨 功能说明

### 已实现功能

1. **登录认证**
   - 支持本地认证和 LDAP 认证
   - JWT Token 自动管理
   - 路由权限守卫

2. **代码库管理**
   - 树形展示代码库和应用
   - 创建/编辑/删除代码库
   - 支持多种 Git 类型（Gitea/GitLab/GitHub）

3. **应用管理**
   - 在代码库下创建应用
   - 创建/编辑/删除应用
   - 支持多种应用类型（Web/API/Job/Microservice）

4. **国际化**
   - 支持中文和英文切换
   - 语言偏好本地持久化

### UI 特点

- ✨ 扁平化设计
- 🎴 卡片式布局
- 🔘 圆角元素
- 🖥️ Mac 风格
- 🌗 响应式设计

## 📂 项目结构

```
src/
├── assets/              # 静态资源
├── components/          # 通用组件
│   ├── Layout/         # 主布局
│   └── PrivateRoute/   # 路由守卫
├── pages/              # 页面组件
│   ├── Login/          # 登录页
│   └── Repository/     # 代码库管理页
├── services/           # API 服务
│   ├── auth.ts
│   ├── repository.ts
│   └── application.ts
├── stores/             # 状态管理
│   └── authStore.ts
├── types/              # TypeScript 类型
│   └── index.ts
├── utils/              # 工具函数
│   ├── request.ts      # HTTP 请求封装
│   └── token.ts        # Token 管理
├── locales/            # 国际化
│   ├── zh.ts           # 中文
│   ├── en.ts           # 英文
│   └── index.ts
├── routes/             # 路由配置
│   └── index.tsx
├── App.tsx             # 根组件
└── main.tsx            # 入口文件
```

## 🔌 后端对接

确保后端服务已启动：

```bash
# 在项目根目录
cd cmd/base
go run main.go
```

后端服务地址：http://localhost:8080

## 📖 开发说明

### 添加新页面

1. 在 `src/pages/` 下创建新目录
2. 在 `src/routes/index.tsx` 中添加路由
3. 在 `src/components/Layout/index.tsx` 中添加菜单项

### 添加新 API

1. 在 `src/types/index.ts` 中定义类型
2. 在 `src/services/` 中创建服务文件
3. 使用 React Query 的 `useQuery` 或 `useMutation`

### 添加国际化文本

1. 在 `src/locales/zh.ts` 中添加中文
2. 在 `src/locales/en.ts` 中添加英文
3. 使用 `t('key')` 引用

## ⚠️ 注意事项

1. **开发环境热重载**：修改代码后会自动刷新
2. **API 代理**：确保后端服务在 8080 端口运行
3. **Token 过期**：Token 过期后会自动跳转到登录页
4. **浏览器兼容**：建议使用 Chrome/Edge/Safari 最新版

## 🐛 常见问题

### 1. 依赖安装失败

```bash
# 清除缓存后重试
npm cache clean --force
npm install
```

### 2. 端口被占用

修改 `vite.config.ts` 中的端口：

```typescript
server: {
  port: 5174, // 改成其他端口
}
```

### 3. API 请求失败

检查：
- 后端服务是否启动
- 端口是否正确（8080）
- 浏览器控制台是否有 CORS 错误

### 4. 登录后立即退出

检查：
- 后端 JWT Secret 配置
- Token 是否正确保存
- 浏览器控制台是否有错误

## 📞 技术支持

如有问题，请查看：
- [React 文档](https://react.dev/)
- [Ant Design 文档](https://ant.design/)
- [Vite 文档](https://vitejs.dev/)

---

**Happy Coding! 🎉**

